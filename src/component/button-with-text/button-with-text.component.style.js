import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {
    borderRadius: 70,
    height: 60,
  },
  buttonText: {
    textAlign: 'center',
    fontSize: 18,
    lineHeight: 60,
  },
});
